<%= form_with model: @message do |form| %>
  <div>
    <%= form.label :chat_id, "Chat" %>
    <%= form.select :chat_id, @chats.collect { |c| ["Chat ##{c.id}", c.id] }, prompt: "Select Chat" %>
  </div>

  <div>
    <%= form.label :body, "Message" %>
    <%= form.text_area :body, placeholder: "Enter message" %>
  </div>

  <div>
    <%= form.submit "Save" %>
  </div>
<% end %>
